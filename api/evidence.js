"use strict";
const {authorize}=require("../server/auth"),{productionPrivateFileStorage}=require("../server/private-file-storage"),{send,method,wrap}=require("../server/http");
function createHandler({storage,secretValue}={}){return wrap(async(req,res)=>{method(req,"POST");const claims=authorize(req,"OWN_FILE_WRITE",{secretValue}),body=typeof req.body==="string"?JSON.parse(req.body):req.body||{};if(body.ownerId||body.role)throw Object.assign(new Error("OWNER_IS_SESSION_CONTROLLED"),{statusCode:400});const result=await(storage||productionPrivateFileStorage()).put({ownerId:claims.sub,purpose:body.purpose,originalName:body.originalName,mimeType:body.mimeType,bytes:Buffer.from(body.base64||"","base64")});return send(res,201,{evidenceId:result.evidenceId,mimeType:result.mimeType,size:result.size,createdAt:result.createdAt});});}
module.exports=createHandler();module.exports.createHandler=createHandler;
