"use strict";
const SOURCES=Object.freeze(["IOS_NATIVE","ANDROID_NATIVE","WEB_GEOLOCATION","MANUAL_ADDRESS","GEOCODED_ADDRESS"]);
function normalizeLocation(coords,source){if(!SOURCES.includes(source))throw new Error("Fonte de localização inválida.");const latitude=Number(coords.latitude),longitude=Number(coords.longitude),accuracyMeters=Number(coords.accuracy??coords.accuracyMeters);if(!Number.isFinite(latitude)||latitude < -90||latitude > 90||!Number.isFinite(longitude)||longitude < -180||longitude > 180)throw new TypeError("Coordenadas inválidas.");return{latitude,longitude,accuracyMeters:Number.isFinite(accuracyMeters)?accuracyMeters:null,capturedAt:new Date(coords.timestamp||Date.now()).toISOString(),locationSource:source};}
class NativeLocationAdapter{constructor({platform,getCurrentPosition}){this.source=platform==="ios"?"IOS_NATIVE":platform==="android"?"ANDROID_NATIVE":"WEB_GEOLOCATION";this.getCurrentPosition=getCurrentPosition;}async captureOnce({consent=false}={}){if(!consent)throw new Error("Consentimento de localização obrigatório.");const value=await this.getCurrentPosition({enableHighAccuracy:true,timeout:10000,maximumAge:30000});return normalizeLocation(value.coords||value,this.source);}}
module.exports={SOURCES,normalizeLocation,NativeLocationAdapter};
