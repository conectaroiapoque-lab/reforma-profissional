"use strict";
const ENTRY_TYPES=Object.freeze(["CUSTOMER_CHARGE","PROVIDER_PAYABLE","PLATFORM_REVENUE","PAYMENT_FEE","TAX","TRAVEL_BONUS","REFUND","MATERIAL_COST","MATERIAL_SALE","PROVIDER_PAYOUT"]);
function createFinancialLedger(orderId,snapshot,{materialAmountCents=0,currency="BRL"}={}){const rows=[["CUSTOMER_CHARGE",snapshot.customerPriceCents],["PROVIDER_PAYABLE",snapshot.providerTotalPayoutCents],["PLATFORM_REVENUE",snapshot.platformGrossRevenueCents],["PAYMENT_FEE",snapshot.paymentFeesCents||0],["TAX",snapshot.taxesCents||0],["TRAVEL_BONUS",snapshot.travelBonusCents||0],["MATERIAL_SALE",materialAmountCents]].filter(([,amount])=>amount);return rows.map(([type,amountCents],i)=>({ledgerEntryId:`${orderId}-${i+1}`,orderId,type,amountCents,currency,status:"PENDING",createdAt:new Date().toISOString()}));}
function reconcile(snapshot){const destinations=snapshot.providerTotalPayoutCents+snapshot.contributionMarginCents+(snapshot.variableCostsCents||0);return{receivedCents:snapshot.customerPriceCents,destinationsCents:destinations,pendingBalanceCents:snapshot.customerPriceCents-destinations,balanced:snapshot.customerPriceCents===destinations};}
module.exports={ENTRY_TYPES,createFinancialLedger,reconcile};
