# Integração eSocial — desenho DRAFT

`legalReviewRequired = true` · `accountingReviewRequired = true`

Estados: `NOT_APPLICABLE`, `PENDING`, `REQUIRED`, `SUBMITTED`, `ACCEPTED`, `REJECTED`. A decisão depende do contratante real, evento vigente e parecer contábil. O adaptador futuro receberá apenas dados necessários, usará credenciais corporativas seguras e **não armazenará senha gov.br**. Recibos, retificações e rejeições serão auditáveis e associados à OS, sem liberar RPA ou retenções antes da aprovação contábil.
