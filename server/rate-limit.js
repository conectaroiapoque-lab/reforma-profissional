"use strict";
const crypto=require("node:crypto");
class RedisRateLimiter{constructor({url=process.env.KV_REST_API_URL,token=process.env.KV_REST_API_TOKEN}={}){if(!url||!token)throw new Error("RATE_LIMIT_STORE_NOT_CONFIGURED");this.url=url.replace(/\/$/,"");this.token=token;}async command(...args){const response=await fetch(this.url,{method:"POST",headers:{authorization:`Bearer ${this.token}`,"content-type":"application/json"},body:JSON.stringify(args)});if(!response.ok)throw new Error("RATE_LIMIT_STORE_UNAVAILABLE");return(await response.json()).result;}async assert({scope,identity,limit=20,windowSeconds=60}){const digest=crypto.createHash("sha256").update(String(identity||"unknown")).digest("hex"),key=`rate:${scope}:${digest}:${Math.floor(Date.now()/(windowSeconds*1000))}`,count=await this.command("INCR",key);if(count===1)await this.command("EXPIRE",key,windowSeconds+1);if(count>limit)throw Object.assign(new Error("RATE_LIMIT_EXCEEDED"),{statusCode:429});}}
module.exports={RedisRateLimiter};
