"use strict";
const {recordAcceptance}=require("../domain/launch-compliance");
const {resolveLegalDocument}=require("./legal-document-registry");
const customerDocument=resolveLegalDocument("CLIENT_TERMS"),providerDocument=resolveLegalDocument("PROVIDER_AGREEMENT");
const CURRENT_CUSTOMER_TERMS_VERSION=customerDocument.version,CURRENT_CUSTOMER_TERMS_HASH=customerDocument.officialHash,CURRENT_PROVIDER_CONTRACT_VERSION=providerDocument.version,CURRENT_PROVIDER_CONTRACT_HASH=providerDocument.officialHash;
function accept({documentType,subjectId,orderId=null,accepted,displayedDocumentVersion,ip,userAgent}){const document=resolveLegalDocument(documentType);if(accepted!==true)throw Object.assign(new Error("LEGAL_ACCEPTANCE_REQUIRED"),{statusCode:400});if(displayedDocumentVersion&&displayedDocumentVersion!==document.version)throw Object.assign(new Error("STALE_LEGAL_DOCUMENT"),{statusCode:409});return recordAcceptance({subjectId,orderId,documentType,documentVersion:document.version,documentHash:document.officialHash,ip,userAgent});}
function recordCustomerTermsAcceptance(input){if(input.accepted!==true)throw Object.assign(new Error("CUSTOMER_TERMS_ACCEPTANCE_REQUIRED"),{statusCode:400});const evidence=accept({documentType:"CLIENT_TERMS",subjectId:input.customerId,orderId:input.orderId,accepted:input.accepted,displayedDocumentVersion:input.displayedDocumentVersion,ip:input.ip,userAgent:input.userAgent});return Object.freeze({customerId:input.customerId,orderId:input.orderId,termsVersion:evidence.documentVersion,termsHash:evidence.documentHash,acceptedAt:evidence.acceptedAt,acceptanceEvidenceId:evidence.acceptanceEvidenceId,ipHash:evidence.ipHash,userAgentHash:evidence.userAgentHash});}
function recordProviderContractAcceptance(input){if(input.accepted!==true)throw Object.assign(new Error("PROVIDER_CONTRACT_ACCEPTANCE_REQUIRED"),{statusCode:400});const evidence=accept({documentType:"PROVIDER_AGREEMENT",subjectId:input.providerId,accepted:input.accepted,displayedDocumentVersion:input.displayedDocumentVersion,ip:input.ip,userAgent:input.userAgent});return Object.freeze({providerId:input.providerId,contractVersion:evidence.documentVersion,contractHash:evidence.documentHash,acceptedAt:evidence.acceptedAt,acceptanceEvidenceId:evidence.acceptanceEvidenceId,ipHash:evidence.ipHash,userAgentHash:evidence.userAgentHash});}
module.exports=Object.freeze({CURRENT_CUSTOMER_TERMS_VERSION,CURRENT_CUSTOMER_TERMS_HASH,CURRENT_PROVIDER_CONTRACT_VERSION,CURRENT_PROVIDER_CONTRACT_HASH,accept,recordCustomerTermsAcceptance,recordProviderContractAcceptance});
